# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Firebase debug logs
/firebase-debug.log
/firebase-debug.*.log
*.cache
hosting/.cache

# Firebase Emulators
/firebase-*.zip
/.firebase/
/emulator-ui/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (local configs) — keep .env.example committed
.env
.env.local
.env.*.local

# Editor
.DS_Store
.vscode/*
!.vscode/extensions.json

# NOTE: firebase.json, .firebaserc, and firestore.rules ARE committed on purpose so the
# project config and security rules are versioned. Only secrets (.env.local) stay local.
